/** Hero background layer — optional effects (hero uses flat bg in Home) */
const HeroBackdrop = () => (
  <div className="pointer-events-none absolute inset-0 overflow-hidden" aria-hidden />
);

export default HeroBackdrop;
